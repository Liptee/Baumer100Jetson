# Optional dependency.
# Needed only for --backend opencv path in tools/baumer_record_headless.py.
opencv-python>=4.10

# API service
fastapi>=0.110
uvicorn>=0.29

# Optional dependency.
# Needed only for telemetry capture (--telemetry-enable).
pymavlink>=2.4.41
